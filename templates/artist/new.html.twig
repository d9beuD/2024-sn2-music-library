{% extends 'base.html.twig' %}

{% block title %}New Artist{% endblock %}

{% block body %}
    <h1>Create new Artist</h1>

    {{ include('artist/_form.html.twig') }}
    
    <button type="submit" form="artist-form" class="btn btn-primary">Save</button>

    <a class="btn btn-secondary" href="{{ path('app_artist_index') }}">back to list</a>
{% endblock %}
